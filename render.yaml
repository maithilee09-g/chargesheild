services:
  - type: web
    name: chargeshield-ai
    env: docker
    plan: standard
    dockerfilePath: Dockerfile
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DATABASE
        value: chargeshield
      - key: PORT
        value: 8000
