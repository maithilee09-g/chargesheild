version: '3.8'

services:
  chargeshield:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - MONGODB_URI=${MONGODB_URI}
      - MONGODB_DATABASE=chargeshield
      - PORT=8000
    restart: unless-stopped
